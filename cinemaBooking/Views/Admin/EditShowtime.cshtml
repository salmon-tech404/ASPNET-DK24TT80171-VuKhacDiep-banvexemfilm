@model cinemaBooking.Models.ViewModels.ShowtimeFormViewModel
@{
    ViewData["Title"] = "Sửa suất chiếu";
}

<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-md-2">@await Html.PartialAsync("_AdminSidebar")</div>
        <div class="col-md-10">
            <h3 class="fw-bold mb-4"><i class="fas fa-edit me-2 text-danger"></i>Sửa suất chiếu</h3>
            <div class="card">
                <div class="card-body">
                    <form asp-action="EditShowtime" asp-route-id="@Model.Id" method="post">
                        @Html.AntiForgeryToken()
                        @await Html.PartialAsync("_ShowtimeFormPartial", Model)
                        <div class="mt-3">
                            <button type="submit" class="btn btn-danger"><i class="fas fa-save me-2"></i>Lưu thay đổi</button>
                            <a asp-action="Showtimes" class="btn btn-outline-secondary ms-2">Hủy</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }
